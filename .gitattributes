# The pre-commit hook runs through git's bundled sh, which cannot execute a script with
# CRLF line endings. Force LF on checkout regardless of the local core.autocrlf setting.
scripts/hooks/pre-commit text eol=lf
